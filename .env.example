NODE_ENV=development
PORT=5006
TICKET_DEFAULT_SLA_HOURS=48
DEFAULT_TICKET_ADMIN_EMAIL=admin@serveaso.com

# --- Same as payments service (required on Render if not using monorepo payments/.env) ---
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Optional pool tuning (same names as payments)
# POSTGRES_POOL_MAX=10
# POSTGRES_POOL_IDLE_TIMEOUT_MS=60000
# POSTGRES_POOL_CONNECTION_TIMEOUT_MS=10000

# Same secret as utils ADMIN_PUSH_SECRET for admin UI
ADMIN_TICKET_SECRET=
